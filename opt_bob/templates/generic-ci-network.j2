network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses: [ {{ ip }}/{{ cidr }} ]
      routes:
      - to: 0.0.0.0/0
        via: {{ gateway }}
      nameservers:
        addresses:
        - {{ nameservers }}
        search:
        - {{ dns_domain }}
